---
import Layout from '../layouts/Layout.astro';
import ProfileWrapper from './_ProfileWrapper.tsx';

const pathname = Astro.url.pathname;
---

<Layout pathname={pathname}>
  <ProfileWrapper client:only="react" />
</Layout>
