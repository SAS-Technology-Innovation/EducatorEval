---
import Layout from '../layouts/Layout.astro';
import ScheduleWrapper from './_ScheduleWrapper.tsx';

const pathname = Astro.url.pathname;
---

<Layout pathname={pathname}>
  <ScheduleWrapper client:only="react" />
</Layout>