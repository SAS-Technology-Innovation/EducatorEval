---
import Layout from '../layouts/Layout.astro';
import ObserveWrapper from './_ObserveWrapper.tsx';

const pathname = Astro.url.pathname;
---

<Layout pathname={pathname}>
  <ObserveWrapper client:only="react" />
</Layout>
